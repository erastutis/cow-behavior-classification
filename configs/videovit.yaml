seed: 42

data:
  annotations_path: data/CBVD-5/annotations.csv
  manifest_path: data/processed/manifest.csv

model:
  name: videovit
  image_size: 224
  clip_size: 8

training:
  epochs: 20
  batch_size: 4
  learning_rate: 0.00005
  weight_decay: 0.0001
  num_workers: 2
  output_dir: models/videovit
